# Wire Contract Fixtures

These fixtures are canonical examples for the Strands HTTP Wire Contract v1 described in `docs/wire-contract.md`.

They are not raw sdk-python fixtures. They describe the PHP-facing JSON/SSE emitted by wrapper services built on top of sdk-python.

When a wrapper intentionally changes this contract, update these fixtures, `docs/wire-contract.md`, and the relevant ADR together.
