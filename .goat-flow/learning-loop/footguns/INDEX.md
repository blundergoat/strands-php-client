---
category: index
bucket: footguns
generated: true
---

# Footguns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one active footgun entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Interrupt invoke responses may have empty text](contract.md) (search: "## Footgun: Interrupt invoke responses may have empty text") - Fixture smoke tests or parser assertions fail when they assume every `invoke-response-*.json` fixture must contain non-empty `text`.
- [Media content blocks need a top-level `format` field](contract.md) (search: "## Footgun: Media content blocks need a top-level `format` field") - A new `AgentInput::with*` helper for an image, document, or video block looks correct in tests (source structure matches, media_type matches) but the wrapper rejects the payload, or accepts it…
- [Python Gateway Smoke Checks Create Cache Files](generated-files.md) (search: "## Footgun: Python Gateway Smoke Checks Create Cache Files") - After running Python gateway smoke checks or `python -m py_compile`, `git status` shows untracked `__pycache__/` directories or `.pyc` files under `examples/python-gateway/`.
- [`naming.parameter-type-name` allowlist ignores local variables](gruff-php.md) (search: "## Footgun: `naming.parameter-type-name` allowlist ignores local variables") - The `ignoredParameterNames` option on `naming.parameter-type-name` only filters function/method parameters.
- [Renaming public ctor / method params breaks named-argument callers](gruff-php.md) (search: "## Footgun: Renaming public ctor / method params breaks named-argument callers") - `naming.parameter-type-name` will tell you to rename `$config` to `$strandsConfig` on `StrandsClient::__construct`.
- [gruff baseline file is all-or-nothing](gruff-php.md) (search: "## Footgun: gruff baseline file is all-or-nothing") - `gruff-php analyse --generate-baseline` captures every current finding as accepted debt.
- [`test-quality.mystery-guest` flags `file_get_contents` only in test bodies, not helpers](gruff-php.md) (search: "## Footgun: `test-quality.mystery-guest` flags `file_get_contents` only in test bodies, not helpers") - The rule's escape hatch is structural: it walks `TestQualityNodeHelper::testScopes($analysisUnit)` and flags read functions there.
- [PHP variable rename via plain string replace eats prefixes](gruff-php.md) (search: "## Footgun: PHP variable rename via plain string replace eats prefixes") - A literal `$auth` → `$apiKeyAuth` replace will turn `$author` into `$apiKeyAuthor` and `$auth1` into `$apiKeyAuth1`.
- [Hook policy fixes regress silently unless the self-test encodes them](hooks.md) (search: "## Footgun: Hook policy fixes regress silently unless the self-test encodes them") - Trap.
- [Symfony DependencyInjection has its own StrandsClientFactory subclass](integration.md) (search: "## Footgun: Symfony DependencyInjection has its own StrandsClientFactory subclass") - `src/Integration/Symfony/DependencyInjection/StrandsClientFactory.php` extends `src/Integration/StrandsClientFactory.php`.
- [StrandsClient + StreamResult co-change frequently](integration.md) (search: "## Footgun: StrandsClient + StreamResult co-change frequently") - `src/StrandsClient.php` and `tests/Unit/StrandsClientStreamTest.php` have both been modified in 8 of the last ~20 commits.
- [RequestMiddleware Cannot Observe Parsed Results](observability.md) (search: "## Footgun: RequestMiddleware Cannot Observe Parsed Results") - Telemetry changes try to attach token usage, stop reasons, stream event counts, or parsed tool names inside `RequestMiddleware::afterResponse()`.
- [Cross-interface auto-configuration double-fires observers](observability.md) (search: "## Footgun: Cross-interface auto-configuration double-fires observers") - A `ResponseObserver` registered via Symfony DI fires `afterInvoke` / `afterStream` / `afterResponse` twice per request.
- [afterResponse skipped when buildRequest throws](observability.md) (search: "## Footgun: afterResponse skipped when buildRequest throws") - A `RequestMiddleware` whose `beforeRequest()` acquired some state (active OTel scope, lock, counter increment) never sees `afterResponse()` when auth or a sibling middleware later throws.
- [`HttpTransport` is a PHP `interface`, not an abstract class](transport-and-streaming.md) (search: "## Footgun: `HttpTransport` is a PHP `interface`, not an abstract class") - `src/Http/HttpTransport.php` (search: `interface HttpTransport`) declares both `post()` and `stream()` with no default bodies — PHP `interface` types cannot ship implementations.
- [`StreamParser` skips unknown events; `StreamEvent::fromArray()` throws on them](transport-and-streaming.md) (search: "## Footgun: `StreamParser` skips unknown events; `StreamEvent::fromArray()` throws on them") - `src/Streaming/StreamParser.php` (search: `skippedEvents++`) silently increments `skippedEvents` for unrecognised SSE types, while `src/Streaming/StreamEvent.php` (search: `throw new…
- [Stream cancellation requires literal `false` from the callback](transport-and-streaming.md) (search: "## Footgun: Stream cancellation requires literal `false` from the callback") - `src/Http/SymfonyHttpTransport.php` (search: `if ($onChunk($content) === false)`) calls `$response->cancel()` only when the user callback returns the boolean `false`.
- [`PsrHttpTransport::stream()` is intentionally unimplemented](transport-and-streaming.md) (search: "## Footgun: `PsrHttpTransport::stream()` is intentionally unimplemented") - `src/Http/PsrHttpTransport.php` only supports `post()`.
- [`Usage::fromArray()` is the single canonical hydrator](transport-and-streaming.md) (search: "## Footgun: `Usage::fromArray()` is the single canonical hydrator") - `src/Response/Usage.php` (search: `intField`) owns the defensive type-checking for token counts.
- [`src/StrandsClient.php` is a coordination hot-spot](transport-and-streaming.md) (search: "## Footgun: `src/StrandsClient.php` is a coordination hot-spot") - `src/StrandsClient.php` accumulates four cooperating concerns: retry-with-backoff, middleware ordering, message validation, and stream-cancellation propagation.
- [HttpTransport is an interface, not an abstract class](transport.md) (search: "## Footgun: HttpTransport is an interface, not an abstract class") - `HttpTransport` (search: `interface HttpTransport`) is a PHP interface.
- [PsrHttpTransport.stream() always throws](transport.md) (search: "## Footgun: PsrHttpTransport.stream() always throws") - `PsrHttpTransport::stream()` (search: `'SSE streaming is not supported by PsrHttpTransport'`) unconditionally throws `StrandsException`.
- [Auth runs AFTER middleware by design](transport.md) (search: "## Footgun: Auth runs AFTER middleware by design") - In `StrandsClient::buildJsonRequest()` (search: `buildJsonRequest`), middleware `beforeRequest()` runs first, then `AuthStrategy::authenticate()`.
- [StreamParser 10 MB buffer limit](transport.md) (search: "## Footgun: StreamParser 10 MB buffer limit") - `StreamParser` (search: `MAX_BUFFER_SIZE`) throws `StreamInterruptedException` if the buffer exceeds 10 MB without encountering a complete SSE event delimiter (`\n\n`).
- [PsrHttpTransport silently ignores timeout parameters](transport.md) (search: "## Footgun: PsrHttpTransport silently ignores timeout parameters") - `PsrHttpTransport::post()` (search: `'PsrHttpTransport does not support timeout parameters'`) accepts `$timeout` and `$connectTimeout` parameters but ignores them — logging a notice once.
