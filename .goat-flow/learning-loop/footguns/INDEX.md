---
category: index
bucket: footguns
generated: true
---

# Footguns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one active footgun entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Symfony 8 cannot be added to the CI matrix, and the blocker is dev tooling](ci.md) (search: "## Footgun: Symfony 8 cannot be added to the CI matrix, and the blocker is dev tooling") - `composer.json` allows `symfony/*: ^6.4 || ^7.0 || ^8.0` and the README advertises it, but…
- [Interrupt invoke responses may have empty text](contract.md) (search: "## Footgun: Interrupt invoke responses may have empty text") - Fixture smoke tests or parser assertions fail when they assume every `invoke-response-*.json`…
- [Media content blocks need a top-level `format` field](contract.md) (search: "## Footgun: Media content blocks need a top-level `format` field") - A new `AgentInput::with*` helper for an image, document, or video block looks correct in tests…
- [Python Gateway Smoke Checks Create Cache Files](generated-files.md) (search: "## Footgun: Python Gateway Smoke Checks Create Cache Files") - After running Python gateway smoke checks or `python -m py_compile`, `git status` shows untracked…
- [Renaming public ctor / method params breaks named-argument callers](gruff-php.md) (search: "## Footgun: Renaming public ctor / method params breaks named-argument callers") - Any naming rule that suggests renaming `$config` to `$strandsConfig` on…
- [gruff baseline file is all-or-nothing](gruff-php.md) (search: "## Footgun: gruff baseline file is all-or-nothing") - `gruff-php analyse --generate-baseline` captures every current finding as accepted debt.
- [`test-quality.mystery-guest` flags `file_get_contents` only in test bodies, not helpers](gruff-php.md) (search: "## Footgun: `test-quality.mystery-guest` flags `file_get_contents` only in test bodies, not helpers") - The rule's escape hatch is structural: it walks `TestQualityNodeHelper::testScopes($analysisUnit)`…
- [PHP variable rename via plain string replace eats prefixes](gruff-php.md) (search: "## Footgun: PHP variable rename via plain string replace eats prefixes") - A literal `$auth` → `$apiKeyAuth` replace turns `$author` into `$apiKeyAuthor` and `$auth1` into…
- [the deny-dangerous git guard is deliberately patched away from the published template](hooks.md) (search: "## Footgun: the deny-dangerous git guard is deliberately patched away from the published template") - `goat-flow audit . --harness` reports `Skill Template Drift: FAIL` naming…
- [Hook policy fixes regress silently unless the self-test encodes them](hooks.md) (search: "## Footgun: Hook policy fixes regress silently unless the self-test encodes them") - Trap.
- [a duplicated hook registration passes `goat-flow audit` as effective](hooks.md) (search: "## Footgun: a duplicated hook registration passes `goat-flow audit` as effective") - `goat-flow audit . --harness --agent claude` reports `post-turn-safety` as `effective` with…
- [`deny-dangerous` scans heredoc bodies as command syntax](hooks.md) (search: "## Footgun: `deny-dangerous` scans heredoc bodies as command syntax") - A `Bash` call that pipes a markdown document through a heredoc (for example `cat <<'EOF' |…
- [Symfony DependencyInjection has its own StrandsClientFactory subclass](integration.md) (search: "## Footgun: Symfony DependencyInjection has its own StrandsClientFactory subclass") - `src/Integration/Symfony/DependencyInjection/StrandsClientFactory.php` is an empty compatibility…
- [Streaming changes cross client, DTO, and test boundaries](integration.md) (search: "## Footgun: Streaming changes cross client, DTO, and test boundaries") - In the 20 commits ending at `a4e30ff`, five modified both `src/StrandsClient.php` and…
- [`stats --check` validates only one semantic-anchor form](learning-loop.md) (search: "## Footgun: `stats --check` validates only one semantic-anchor form") - `goat-flow stats --check` returns `"status": "pass"` and `"totalStaleRefs": 0` while entries in the…
- [`plans check` parses milestone continuation lines as unestimated items](learning-loop.md) (search: "## Footgun: `plans check` parses milestone continuation lines as unestimated items") - `goat-flow plans check <dir> --strict` reports `N task(s)/testing gate item(s) missing an (est:…
- [a bucket with no entry headings is counted but never indexed](learning-loop.md) (search: "## Footgun: a bucket with no entry headings is counted but never indexed") - A learning-loop file is present, well written, and passes `stats --check`, but no agent following…
- [RequestMiddleware Cannot Observe Parsed Results](observability.md) (search: "## Footgun: RequestMiddleware Cannot Observe Parsed Results") - Telemetry changes try to attach token usage, stop reasons, stream event counts, or parsed tool…
- [Cross-interface auto-configuration double-fires observers](observability.md) (search: "## Footgun: Cross-interface auto-configuration double-fires observers") - A `ResponseObserver` registered via Symfony DI fires `afterInvoke` / `afterStream` /…
- [`goat-flow install --force` rewrites project-owned policy that the dry-run preview never lists](setup.md) (search: "## Footgun: `goat-flow install --force` rewrites project-owned policy that the dry-run preview never lists") - A forced install reports success and the expected managed-file count, but hand-written security…
- [`HttpTransport` is a PHP `interface`, not an abstract class](transport-and-streaming.md) (search: "## Footgun: `HttpTransport` is a PHP `interface`, not an abstract class") - `src/Http/HttpTransport.php` (search: `interface HttpTransport`) declares both `post()` and…
- [`StreamParser` skips unknown events; `StreamEvent::fromArray()` throws on them](transport-and-streaming.md) (search: "## Footgun: `StreamParser` skips unknown events; `StreamEvent::fromArray()` throws on them") - `src/Streaming/StreamParser.php` (search: `skippedEvents++`) silently increments `skippedEvents`…
- [Stream cancellation requires literal `false` from the callback](transport-and-streaming.md) (search: "## Footgun: Stream cancellation requires literal `false` from the callback") - `src/Http/SymfonyHttpTransport.php` (search: `if ($onChunk($content) === false)`) calls…
- [`PsrHttpTransport::stream()` is intentionally unimplemented](transport-and-streaming.md) (search: "## Footgun: `PsrHttpTransport::stream()` is intentionally unimplemented") - `src/Http/PsrHttpTransport.php` only supports `post()`; `stream()` (search: `SSE streaming is not…
- [`PsrHttpTransport` silently ignores timeout parameters](transport-and-streaming.md) (search: "## Footgun: `PsrHttpTransport` silently ignores timeout parameters") - `PsrHttpTransport::post()` (search: `does not support timeout parameters`) accepts `$timeout` and…
- [Auth runs AFTER middleware by design](transport-and-streaming.md) (search: "## Footgun: Auth runs AFTER middleware by design") - `StrandsClient::buildRequest()` (search: `private function buildRequest`) and…
- [`StreamParser` 10 MB buffer limit rejects oversized chunks whole](transport-and-streaming.md) (search: "## Footgun: `StreamParser` 10 MB buffer limit rejects oversized chunks whole") - `StreamParser::feed()` (search: `strlen($this->buffer) + strlen($chunk)`) checks the combined byte…
- [`Usage::fromArray()` is the single canonical hydrator](transport-and-streaming.md) (search: "## Footgun: `Usage::fromArray()` is the single canonical hydrator") - `src/Response/Usage.php` (search: `intField`) owns the defensive type-checking for token counts.
- [`src/StrandsClient.php` is a coordination hot-spot](transport-and-streaming.md) (search: "## Footgun: `src/StrandsClient.php` is a coordination hot-spot") - As of `a4e30ff`, repository history contains 18 commits touching `src/StrandsClient.php`, 11…
