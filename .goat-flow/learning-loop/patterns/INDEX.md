---
category: index
bucket: patterns
generated: true
---

# Patterns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one pattern entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [BC-safe naming-rule cleanup workflow](gruff-php.md) (search: "## Pattern: BC-safe naming-rule cleanup workflow") - When a gruff `naming.*` rule returns dozens or hundreds of findings, and the project ships a public…
- [Word-boundary PHP variable rename script](gruff-php.md) (search: "## Pattern: Word-boundary PHP variable rename script") - When you need to rename a PHP variable across many files without corrupting prefix-matching names…
- [Load test fixtures via a private helper method (mystery-guest workaround)](gruff-php.md) (search: "## Pattern: Load test fixtures via a private helper method (mystery-guest workaround)") - When tests need fixture data from `tests/Fixtures/` and `test-quality.mystery-guest` flags the…
- [Use real PSR-17 factories in PSR-18 tests to dodge excessive-mocking](gruff-php.md) (search: "## Pattern: Use real PSR-17 factories in PSR-18 tests to dodge excessive-mocking") - When `test-quality.excessive-mocking` flags PSR-18 transport tests for using 5–6 collaborators…
- [Per-test data extraction to silence `test-quality.test-longer-than-sut`](gruff-php.md) (search: "## Pattern: Per-test data extraction to silence `test-quality.test-longer-than-sut`") - When `test-longer-than-sut` flags dozens of tests across a file because each test has a long inline…
- [Wave-based cleanup for high-volume rule pillars](gruff-php.md) (search: "## Pattern: Wave-based cleanup for high-volume rule pillars") - When a pillar (e.g.
- [Additive Terminal Observation Surface](observability.md) (search: "## Pattern: Additive Terminal Observation Surface") - Middleware needs safe metadata from parsed invoke responses, accumulated stream results, or raw…
- [Changelog Delta Against origin/main](release.md) (search: "## Pattern: Changelog Delta Against origin/main") - When updating `CHANGELOG.md` for "everything on this branch that is not in `origin/main`."
- [Fixture-based testing with JSON responses and SSE files](testing.md) (search: "## Pattern: Fixture-based testing with JSON responses and SSE files") - When writing tests for `StrandsClient`, transports, or streaming behaviour.
- [Classify wire-contract fixtures before parsing](testing.md) (search: "## Pattern: Classify wire-contract fixtures before parsing") - When adding executable coverage for `tests/Fixtures/wire-contract/`.
