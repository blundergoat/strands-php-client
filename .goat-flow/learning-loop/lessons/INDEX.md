---
category: index
bucket: lessons
generated: true
---

# Lessons Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one lesson entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Don't bulk-rename across the codebase without classifying findings first](gruff-php.md) (search: "## Lesson: Don't bulk-rename across the codebase without classifying findings first") - `naming.parameter-type-name` returned 466 findings in this repo.
- [gruff's token-sequence variant check accepts descriptive name expansions](gruff-php.md) (search: "## Lesson: gruff's token-sequence variant check accepts descriptive name expansions") - When two variables in the same scope both expect the same type-mirrored name (e.g. two `SigV4Auth` instances both wanting to be `$sigV4Auth`), the rule calls `isSpecificDuplicateName` which:
- [`naming.abbreviation-allowlist` is global, not parameter-scoped](gruff-php.md) (search: "## Lesson: `naming.abbreviation-allowlist` is global, not parameter-scoped") - `allowlists.acceptedAbbreviations` in `.gruff-php.yaml` exempts an identifier name everywhere: properties, parameters, AND local variables.
- [not every gruff rule has options — some need refactor or accept-as-debt](gruff-php.md) (search: "## Lesson: not every gruff rule has options — some need refactor or accept-as-debt") - Rules with no config knobs encountered in this repo:
- [`createMock` → `createStub` lowers severity but does NOT clear `test-quality.mock-without-expectation`](gruff-php.md) (search: "## Lesson: `createMock` → `createStub` lowers severity but does NOT clear `test-quality.mock-without-expectation`") - `MockWithoutExpectationRule::isMockCreationExpression` treats `createMock`, `createStub`, `getMockBuilder`, `mock`, `partialMock`, `spy`, and `prophesize` identically.
- [`test-quality.mock-only-test` does not recognise `$mock->expects(...)->with(...)` as a SUT assertion](gruff-php.md) (search: "## Lesson: `test-quality.mock-only-test` does not recognise `$mock->expects(...)->with(...)` as a SUT assertion") - Standard PHPUnit "verify a collaborator was called correctly" tests look like:
- [`test-quality.eager-test` is over-eager on multi-attribute lifecycle tests](gruff-php.md) (search: "## Lesson: `test-quality.eager-test` is over-eager on multi-attribute lifecycle tests") - The rule fires when a test has ≥3 assertions AND ≥2 distinct SUT method calls (`minAssertions` default 3).
- [`security.sensitive-data-logging` flags LLM `token` counts as auth tokens](gruff-php.md) (search: "## Lesson: `security.sensitive-data-logging` flags LLM `token` counts as auth tokens") - `SecurityNodeHelper::hasSensitiveContext()` matches the regex `/(?:api[_-]?key|auth(?:orization)?|cookie|pass(?:word|wd)?|private[_-]?key|secret|token)/i` against variable names, property names, and…
- [`security.dangerous-function-call` flags every `$callable()` syntax in tests](gruff-php.md) (search: "## Lesson: `security.dangerous-function-call` flags every `$callable()` syntax in tests") - `DangerousFunctionCallRule` walks `Expr\FuncCall` nodes; when the call's `name` is not a `Node\Name` (i.e. a variable, array dim fetch, or other expression) and the rule's callable-source tracker…
- [`test-quality.test-longer-than-sut` is mostly a setup-extraction nudge](gruff-php.md) (search: "## Lesson: `test-quality.test-longer-than-sut` is mostly a setup-extraction nudge") - The rule fires when a test method body is ≥12 non-blank lines AND has exactly one apparent SUT call.
- [`test-quality.magic-number-assertion` doesn't recognise LLM `usage` property names](gruff-php.md) (search: "## Lesson: `test-quality.magic-number-assertion` doesn't recognise LLM `usage` property names") - The rule fires on `$this->assertSame(<literal>, <expr>)` when the literal isn't in `allowedLiterals` (config) AND the expression's property/key name isn't in the hardcoded `CONTEXTUAL_NUMERIC_NAMES`…
- [`sensitive-data.high-entropy-string` flags long MIME types and rule references](gruff-php.md) (search: "## Lesson: `sensitive-data.high-entropy-string` flags long MIME types and rule references") - The detector matches string literals ≥32 chars in `[A-Za-z0-9_+\/=.-]` that aren't recognised as URLs/routes/known file extensions.
- [Pin library dependency constraints across multiple major versions](history.md) (search: "## Lesson: Pin library dependency constraints across multiple major versions") - The `psr/log` constraint was originally too narrow (one major version), which immediately blocked installation in projects that had already bumped to a newer PSR-3 major.
- [Treat a release as the moment to harden — not the moment to ship and move on](history.md) (search: "## Lesson: Treat a release as the moment to harden — not the moment to ship and move on") - The `918587a Harden v1.4.0: interrupt fail-fast, MIME fixes, auth docs, regression tests` commit bundled four post-feature-cut fixes immediately after the v1.4.0 feature work landed: interrupt…
- [Verify Generated PHPDoc Formatting And Phrasing](verification.md) (search: "## Lesson: Verify Generated PHPDoc Formatting And Phrasing") - While fixing Gruff `docs.missing-public-phpdoc` findings, the first automated PHPDoc insertion pass left double-indented docblocks and awkward summaries such as "Create create mock transport" before…
- [Patch Repeated Assertions By Semantic Context](verification.md) (search: "## Lesson: Patch Repeated Assertions By Semantic Context") - While updating `Usage` tests for numeric-string parsing, repeated `$response->usage->outputTokens` assertions caused the wrong test expectations to be patched twice before the focused test run caught…
- [Verify automated-reviewer "addressed" tags against the file](verification.md) (search: "## Lesson: Verify automated-reviewer") - While triaging CodeRabbit feedback on PR #6, a comment about an SSE-fixture extension claim (`sse-*.txt` vs `.sse`) carried the trailing annotation "✅ Addressed in commit 6299334".
- [Smoke-Test Manually Authored Hook Commands Before Audit](verification.md) (search: "## Lesson: Smoke-Test Manually Authored Hook Commands Before Audit") - While registering the Antigravity deny hook, the first `.agents/hooks.json` command string started `bash -c '` but missed the final closing quote.
